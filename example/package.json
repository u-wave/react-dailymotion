{
  "private": true,
  "name": "react-dailymotion-example",
  "description": "React-Dailymotion example.",
  "version": "0.0.0-example",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "build": "browserify app.js -t babelify > bundle.js"
  },
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "babelify": "^9.0.0",
    "browserify": "^16.2.2"
  }
}
